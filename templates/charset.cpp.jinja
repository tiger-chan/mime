#include <mime/mime_details.hpp>

namespace mime {
	namespace details {
		const int32 default_charsets_size{ {{ charset | length }} };
		const char *default_charsets[] = {

		{%- for item in charset %}
			"{{item}}",
		{% endfor %}};
	}    // namespace details
}    // namespace mime
