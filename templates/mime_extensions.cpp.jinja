#include <mime/mime_details.hpp>

namespace mime {
	namespace details {
		const int32 extensions_size{ {{ types | length }} };
		const char *extensions[] = {
		{% for row in extensions | batch(10) -%}
			{%- for item in row -%} "{{item.value}}", {% endfor %}
		{% endfor %}};
	}    // namespace details
}    // namespace mime
