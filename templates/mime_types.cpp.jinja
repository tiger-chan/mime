#include <mime/mime_details.hpp>

namespace mime {
	namespace details {
		const int32 default_types_size{ {{types | length}} };
		const char *default_types[] = {
		{% for item in types -%}
			"{{item.type}}",
		{% endfor %}};
	}
}
